import NotifyMeForm from "../components/NotifyMeForm";

export default function NotifyMeTest() {
  return (
    <div
      style={{
        minHeight: "100vh",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        padding: 24,
        background: "#0F1F3D",
      }}
    >
      <NotifyMeForm />
    </div>
  );
}
